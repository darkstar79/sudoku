# Python dependencies for development tooling
# Install with: pip install -r requirements.txt
# Or with uv: uv venv && uv pip install -r requirements.txt

# C++ package manager (see README for build instructions)
conan>=2.18.1

# Build tools. Conan also provides cmake via tool_requires, but the repo's
# CMakePresets.json uses the Ninja generator, so ninja must be on PATH.
# Pinning both via pip keeps dev environments reproducible without relying
# on system packages.
cmake>=3.28
ninja>=1.11

# Patch-coverage gate. CI (.github/workflows/ci.yml) runs diff-cover on the
# src/-only coverage.xml so changed lines must be >=80% covered; listed here so
# the same check is reproducible locally:
#   ./scripts/coverage.sh xml
#   diff-cover coverage.xml --compare-branch=origin/main --fail-under=80
# (gcovr is installed inline by CI and intentionally not pinned here.)
diff-cover>=9.0
